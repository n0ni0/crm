{% extends 'layout.html.twig' %}

{% block content%}
  <div id="customerTitle" class="container">
    <h2>Clientes</h2>
  </div>
  <div class="container-fluid">
    <div class="widget-content">
      <table id="customers" class="table table-striped table-bordered table-hover">
        <thead>
          <tr>	
            <th>Foto</th>
            <th>Nombre</th>
            <th>Apellidos</th>
            <th>Teléfono</th>
            <th>Móvil</th>
            <th>Email</th>
            <th>Ver perfíl</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            {% for customers in customer %}
              <td>{{ customers.photo}}</td>
              <td>{{ customers.name}}</td>
              <td>{{ customers.lastname }}</td>
              <td>{{ customers.phone }}</td>
              <td>{{ customers.mobilephone }}</td>
              <td>{{ customers.email }}</td>
              <td><button width="auto" type="button" class="btn btn-primary">Perfíl</button></td>
          </tr>
            {% endfor %}
          <tr>
        </tbody>
      </table>
    </div>
  </div>
{% endblock %}



