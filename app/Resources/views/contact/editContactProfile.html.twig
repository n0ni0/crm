{% extends 'layout.html.twig' %}
z
  {% block panel_title %}
      Editar contacto
  {% endblock %}

  {% block panel_body %}
    <div class="container">
      {{ form_start(form) }}
        {{ form_widget(form) }}
          <input type="submit" value="Editar contacto"
            class="{{ button_css|default("btn btn-success pull-right") }}" />
        {{ form_end(form) }}
    </div>

    {% include 'includes/panelFooter.html.twig' %}
  {% endblock %}

