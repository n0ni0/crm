<div class="commentTextarea container col-md-10">
  {{ form_start(form, {method: 'POST', action: path('newComment', {'task': task.id }) }) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ 'btn.new_comment'|trans({}, 'buttons') }}"
            class="{{ button_css|default("btn btn-sm btn-warning pull-right") }}" />
        {{ form_end(form) }}
 </div>

